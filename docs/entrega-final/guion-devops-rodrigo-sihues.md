# Guion individual — Rodrigo Sihues, Cloud/DevOps

Explicar tras ejecutar/revisar: Docker multi-stage/non-root, Compose y healthchecks; CI separada; secretos/variables; Railway/Vercel y rollback; AWS como futuro no desplegado.

- Archivos: `Dockerfile`, `docker-compose.yml`, `railway.json`, `vercel.json`, workflows y docs de despliegue.
- Pruebas: `docker compose config/build/up` cuando Docker esté disponible; health, rutas SPA y CORS en Preview.
- Commits de preparación existentes (no atribuidos a Rodrigo): backend `fc9869b`, frontend `7ed3676`.
- Commit/PR/revisión personal y URL de Preview: `[PENDIENTE: evidencia real]`.
- Decisión: sin deploy desde CI de integración; promoción requiere aprobación y secrets por ambiente.
